# Aligned with biome.json (space / 2 / lf). Biome is the source of truth for format.
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true
