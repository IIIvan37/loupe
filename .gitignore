node_modules/
dist/
coverage/
reports/
.stryker-tmp/
*.log
.DS_Store
.venv/

# Local scratch media (imported tracks, exported charts) — never project assets
*.mp3
*.pdf

# Modal MVP static token + local env secrets (never commit)
.modal-token.local
*.local
.env*.local
