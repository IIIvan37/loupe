{
  "name": "loupe",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.11.0",
    "pnpm": ">=10.11.0"
  },
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p packages/web/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:mutation": "stryker run",
    "lint": "biome lint .",
    "lint:fix": "biome lint . --write",
    "format": "biome format .",
    "format:fix": "biome format . --write",
    "check": "biome check .",
    "check:fix": "biome check . --write",
    "check:arch": "sheriff verify",
    "check:design": "impeccable detect packages/web/src",
    "check:react": "react-doctor packages/web --blocking warning --no-telemetry",
    "check:dead": "knip",
    "check:dup": "jscpd packages --ignore \"**/public/**\"",
    "gate": "pnpm run \"/^(typecheck|check|check:arch|check:design|check:react|test:coverage|check:dead|check:dup)$/\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "@commitlint/cli": "^21.1.0",
    "@commitlint/config-conventional": "^21.1.0",
    "@softarc/sheriff-core": "^0.19.6",
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/node": "^26.0.1",
    "@vitest/coverage-v8": "^4.0.4",
    "fast-check": "^4.8.0",
    "husky": "^9.1.7",
    "impeccable": "^3.1.0",
    "jscpd": "^5.0.6",
    "knip": "^6.16.1",
    "react-doctor": "^0.5.8",
    "typescript": "^6.0.3",
    "vitest": "^4.0.4"
  }
}
