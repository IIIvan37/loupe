# Cargo workspace (distribution D4.a): the shared download engine and the
# `loupe` server binary (D4.b). One lockfile, one target dir, one sweep.
[workspace]
resolver = "2"
members = ["crates/loupe-download", "crates/loupe-server"]
