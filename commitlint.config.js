// Conventional Commits (short, imperative). Enforced locally by the husky
// `commit-msg` hook and in CI. Tune `rules` per project if needed.
export default {
  extends: ['@commitlint/config-conventional']
}
