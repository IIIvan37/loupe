# Runtime deps, pinned for reproducibility — the server never self-upgrades
# (cf. roadmap A.1: a failed download tells you to `pip install -U yt-dlp`, it
# doesn't do it for you). Bump deliberately: edit a pin, reinstall, re-run the
# gate. yt-dlp especially: update it by hand when extraction breaks.
fastapi==0.138.2
uvicorn[standard]==0.49.0
demucs==4.0.1
torch==2.12.1
torchaudio==2.11.0
# Tempo (beat + downbeat) detection — CPJKU's "Beat This!" transformer, MIT.
# `rotary-embedding-torch` is its one extra runtime dep (pinned for repro).
beat-this==1.1.0
rotary-embedding-torch==0.9.1
yt-dlp==2026.6.9
# Direct dep of the torch-free WAV decoding (app/wav_decode.py) — would arrive
# transitively via torch, but the app imports it itself.
numpy==2.4.6
# Chord estimation (vendored BTC, app/chords.py): CQT features + resampling.
librosa==0.11.0
# Structure detection (vendored SongFormer + MuQ + MusicFM, app/structure.py):
# the SongFormer MSA head + its two SSL backbones and their support libs. Pinned
# to the set the spike validated; transformers is version-sensitive (MuQ/MusicFM
# use its older module layout) and pins huggingface-hub<1.0.
transformers==4.51.1
huggingface-hub==0.30.1
x-transformers==2.23.3
ema-pytorch==0.7.9
omegaconf==2.3.1
safetensors==0.8.0
nnAudio==0.3.4
einops==0.8.2
easydict==1.13
soundfile==0.14.0
