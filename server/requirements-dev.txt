# Test + lint deps, deliberately WITHOUT the heavy ML stack (torch / torchaudio /
# demucs / librosa) so CI and `pytest` run torch-free against the humble-object
# boundary — separation.py / tempo.py are exercised only through their
# absent-capability fallback. To actually run separation/tempo locally, also
# `pip install -r requirements.txt`.
fastapi==0.141.1
uvicorn[standard]==0.52.2
numpy==2.5.2
pytest>=8.0
pytest-cov>=7.1.0
httpx>=0.28.1
ruff>=0.6
pyright>=1.1.411
