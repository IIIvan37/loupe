# Python virtualenv — recreated from requirements.txt, never committed.
.venv/
__pycache__/
*.pyc

# Test/coverage artifacts.
.coverage
.pytest_cache/
htmlcov/

server/.analyze-jwt-secret.local
coverage.xml

# Distribution build artifacts (scripts/build-server-dist.sh) — the web dist
# copied into the package, and the wheel it produces.
app/web_dist/
dist/
