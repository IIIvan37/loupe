# Session — 2026-07-16 — t4-cmd-s

## Done
- **T.4 — Cmd/Ctrl+S = Enregistrer** (roadmap-excellence-4) :
  - **Core (TDD)** : `Command` gagne `{ type: 'saveProject' }`, bindé
    `{ key: 's', meta: true }` **et** `{ key: 's', ctrl: true }` (macOS et
    Windows/Linux) — 3 tests (`Cmd+S`, `Ctrl+S`, `s` nu non bindé).
    Stryker : **key-bindings.ts 112/112 mutants tués**, global 93,74 %.
  - **Carte auto-dérivée** : `describeCommand` gagne le cas
    (`shortcuts.save-project` « Enregistrer le projet ») — les deux lignes
    ⌘+S / ⌃+S apparaissent dans l'empty-state et le dialog « ? » sans autre
    code (invariant N.2 : la doc dérive des bindings).
  - **Garde texte assouplie pour les chords** : un chord Cmd/Ctrl (sans Alt —
    AltGr tape des caractères) traverse `isTextEntry` : Cmd+S enregistre
    depuis le textarea de la grille, là où le réflexe partait dans
    « Enregistrer la page » du navigateur. La garde dialogue reste.
  - **Câblage shell dans `use-shell-shortcuts`** (extrait du shell — budget
    react-doctor 300 lignes tenu) : premier save sous le nom de piste,
    re-save d'un projet dirty sous son nom, **no-op** si projet propre ou
    save en vol (pas de ré-encodage stems redondant). 4 tests shell (save,
    re-save dirty via spy stores, no-op propre, save depuis un champ texte).

## Not done / remaining
- T.5 (aria-invalid BPM/mètre), T.6 (aide format/gestes/AT), T.7 (fine-tune
  cents), T.8 (décisions spectre + EQ à poser à l'utilisateur).

## Decisions
- Cmd+Maj+S retombe volontairement sur `saveProject` (binding
  shift-agnostique — muscle memory « Save As » couverte par le même save).
- Un chord porteur de Cmd/Ctrl (sans Alt) traverse la garde champ-texte du
  listener global — règle générale, pas un cas spécial S.

## Gate status
- typecheck: ✅ (via `pnpm gate`)
- tests (with coverage): ✅ **1544 tests** (+7)
- mutation (Stryker, local, core touché): ✅ **93,74 %** (break 90),
  key-bindings.ts 100 %
- biome / sheriff / knip / jscpd: ✅

## State to resume from
- **Single next action**: PR de `feat/t4-cmd-s`, puis **T.5** — aligner
  `CommitNumberField` (BPM) et le champ mètre sur le motif BarsPerRowField
  (draft + `aria-invalid` au lieu du clamp/rejet silencieux), présentation
  seule.
- Gotchas / half-done edits: aucun.
