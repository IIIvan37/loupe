# Session — 2026-07-16 — w3-faux-bold

## Done
- **W.3 — faux-gras synthétisés** (roadmap-excellence-4) : les trois
  `font-weight: 600` posés sur des fontes qui ne l'embarquent pas.
  - `.title` d'`app-dialog` et de `popover-form` (Space Grotesk — seul
    `500.css` est chargé) abaissés à **500**, avec un commentaire qui
    verrouille la raison. Choix « abaisser » plutôt que « charger 600.css » :
    une seule graisse Space Grotesk dans le bundle, cohérente avec le logo
    du header (déjà en 500), zéro fichier de fonte en plus.
  - `.timeSignature` (Petaluma Script — la fonte n'a que 400) : le
    `font-weight: 600` retiré, le 400 est le rendu authentique de la maquette.
- **Vérif navigateur avant/après** (demandée par la feuille de route) :
  - Dialog « Raccourcis clavier » : computed style confirmé
    `Space Grotesk / 500` ; le 600 forcé via devtools montre le trait
    synthétisé plus empâté.
  - Petaluma : sonde 4/4 côte à côte 400 vs 600 dans la page (fonte chargée
    via `document.fonts.load`) — le 600 synthétisé écrase le trait manuscrit,
    le 400 est net.
- Vérifié qu'aucun autre consommateur de `--font-chart` ne pose de graisse,
  et qu'aucun spec ne pin ces poids.

## Not done / remaining
- W.4 (typo chart sur l'échelle + verrou `font-size` dans
  check-css-tokens.sh) et W.5 (basses groupées : `.kbd` partagé,
  `:active` d'AccountMenu) restent au lot W.

## Decisions
- **Une seule graisse Space Grotesk (500) reste la règle** : tout titre en
  `--font-logo` doit être en 500 tant que `600.css` n'est pas importé dans
  `main.tsx`. Petaluma n'existe qu'en 400 — jamais de `font-weight` dessus.

## Gate status
- typecheck: ✅ (via `pnpm gate`)
- tests (with coverage): ✅ 1537 tests, 126 fichiers — coverage 96,76 % st /
  91,68 % br
- mutation (Stryker, local, if core touched): **skippé** — core intouché
  (diff 100 % CSS `packages/web`)
- biome / sheriff / knip / jscpd: ✅ (jscpd 0,40 %, sous le cliquet 1,0 %)

## State to resume from
- **Single next action**: ouvrir la PR de `feat/w3-faux-bold` puis attaquer
  **W.4** (rabattre `.chartTitle` 1.6rem / `.glyph` 1.5rem sur l'échelle ou
  tokens chart dédiés, + grep bloquant `font-size: [0-9]` dans
  check-css-tokens.sh).
- Gotchas / half-done edits: aucun — arbre propre, diff = 3 fichiers CSS.
