# Session — 2026-07-16 — t8b-stem-eq

## Done
- **T.8b — EQ low/high-cut par stem (décision T.8 : slice BiquadFilter,
  non persistée) — LOT T CLOS** :
  - **Port** : `StemFilter { lowCutHz?, highCutHz? }` + `setStemFilter?()`
    optionnel sur `StemPlaybackEngine` (fakes intouchés).
  - **Adapter** : chaque chaîne stem porte DEUX biquads toujours présents,
    parqués plats (highpass à 0 Hz, lowpass à Nyquist, **Q Butterworth**
    √½ — pas de bosse de résonance au cutoff) — poser un filtre déplace une
    fréquence, ne recâble jamais le graphe (source → gain → highpass →
    lowpass → bus). `desiredFilters` suit le cycle de vie de
    `desiredGains` (posable avant load, oublié au load).
  - **Hook mixer** : `setFilter(id, filter)` — état par tranche exposé sur
    la vue (`channel.filter`), **remis à zéro à chaque mix frais**, jamais
    dans `MixerState` (ce qu'un save persiste). 3 tests hook.
  - **UI** : rangée LC/HC compacte sous le fader de chaque tranche —
    un slider parqué à son bord = « ce côté coupé » (convention DAW d'un
    EQ au repos), sentinelle traduite dans le dumb (`stemFilter()` omet le
    côté au bord). 3 tests composant.
  - **Browser-verify** : filtrage pendant la lecture réelle, chaîne
    intacte, rangées bien alignées dans les tranches.

## Not done / remaining
- **Lot T complet** (T.1–T.8). Roadmap v4 : tout le séquencement validé est
  livré — reste l'évaluation notée v5 quand souhaité.

## Decisions
- EQ = aide d'écoute session-only (décision T.8 confirmée à
  l'implémentation : ni manifest, ni signature, ni MixerState touchés).
- Biquads toujours dans la chaîne (identité aux bords) plutôt qu'insérés à
  la demande — zéro recâblage, zéro pop.

## Gate status
- typecheck: ✅ (via `pnpm gate`)
- tests (with coverage): ✅ **1591 tests** (+6)
- mutation (Stryker): **skippé** — core intouché (port optionnel = types
  seuls ; adapter/hook/UI web)
- biome / sheriff / knip / jscpd: ✅

## State to resume from
- **Single next action**: PR de `feat/t8b-stem-eq`, cocher T.4–T.8 dans la
  roadmap v4, puis **évaluation notée v5** (ou reprendre la veille).
- Gotchas / half-done edits: aucun.
