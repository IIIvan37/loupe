# Session — 2026-07-16 — t7-fine-tune

## Done
- **T.7 — fine-tune de hauteur ±50 cents** (roadmap-excellence-4), séparé de
  la transposition (le flag N.3 et `transposedBy` restent en demi-tons
  entiers — le fine-tune ne participe jamais au modulo 12) :
  - **Core (TDD)** : `clampFineTuneCents` (NaN→0, arrondi au cent, clamp
    ±50, property test) + `fineTuneOrDefault` (lecture « absent ⇔ 0 » du
    manifest, gardée contre la corruption via le clamp — même règle que
    `tuningOrDefault`/`chartTransposedBy`). `ProjectTuning.fineTuneCents?`
    optionnel.
  - **Player** : les moteurs entendent UN pitch (`demi-tons + cents/100` —
    SoundTouch prend le fractionnaire) ; un **ref porte la paire** pour que
    deux setters enchaînés dans un même render (reset d'import, restore)
    n'appliquent jamais l'autre moitié périmée (piège de closure trouvé à
    l'implémentation). `restoreTuning` déplacé dans le player (il possède
    les setters re-clampants) ; reset à 0 sur import frais.
  - **Persistance** : écrit « absent ⇔ 0 » (`tuningSnapshot` module-level),
    signé via `fineTuneOrDefault` (un vieux manifest signe égal), round-trip
    vérifié au niveau shell (save → import frais → reopen → champ à 30).
  - **UI** : champ « Ajustement fin (cents) » dans la barre de transport,
    sur `CommitNumberField` **extrait en `app/ui/`** (le panneau tempo le
    compose désormais depuis là ; flag `isValid` de T.5 inclus — hors ±50
    flaggé). Libellé décoratif `aria-hidden` (l'input porte l'aria-label —
    react-doctor label-has-associated-control ne voit pas la nidification à
    travers un composant). `TransportBar` recâblé via un `ShellFooter`
    module-level (budget 300 lignes du shell tenu, symétrie
    ShellHeader/Main/Dialogs/Footer).

## Not done / remaining
- T.8 — les deux décisions produit (spectre, EQ) à poser à l'utilisateur.

## Decisions
- Fine-tune = **bouton séparé**, jamais dans l'arithmétique de la grille.
- 2 mutants survivants de `fine-tune.ts` documentés **équivalents**
  (frontières `<` vs `<=` : à ±50 exactement les deux branches retournent
  la même valeur) — même forme que `clampPitchSemitones`.

## Gate status
- typecheck: ✅ (via `pnpm gate`)
- tests (with coverage): ✅ **1570 tests** (+14)
- mutation (Stryker, core touché): ✅ **93,63 %** (break 90) —
  `project.ts` 52/52, `fine-tune.ts` 13/15 (2 équivalents documentés)
- biome / sheriff / knip / jscpd: ✅

## State to resume from
- **Single next action**: PR de `feat/t7-fine-tune`, puis **T.8** — poser
  les deux arbitrages (spectre : v1 chroma honnête vs veille datée +
  retrait des onglets vides ; EQ : veille « couvert par la séparation » vs
  slice BiquadFilterNode par stem).
- Gotchas / half-done edits: aucun.
