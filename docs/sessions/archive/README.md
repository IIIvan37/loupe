# Archived session reports

Reports rolled out of the working set (`docs/sessions/` keeps the 5 most recent).
Nothing here is deleted — it is moved, so `docs/sessions/` stays scannable while
the history stays readable.

Older still? `git log docs/sessions/`.
