# Session — 2026-07-14 — icônes trop petites (1em → 1.5em)

## Done

- **Retour utilisateur** : « toutes les icônes sont trop petites ». Cause :
  le composant `Icon` (SVG inline, vocabulaire ✎ ✕ ⏮ ▶ ⟳ ✓) est dimensionné
  en `1em` — il hérite de la police du bouton hôte (`--font-size-s/xs`,
  12–14 px), donc des glyphes minuscules partout.
- **Fix central validé (1,5× partout, AskUserQuestion)** : `Icon` passe à
  `1.5em` (≈ 18–21 px dans les boutons actuels), toujours proportionnel à la
  police de l'hôte — un seul changement, transport / crayons / croix /
  boucle / check suivent.
- Vérif navigateur : transport et actions de la liste Repères nettement plus
  lisibles, la taille des boutons ne bouge pas (l'icône grandit dedans).

## Not done / remaining

- Pas de taille dédiée pour le transport (~24 px, convention DAW) — option
  écartée au profit du 1,5× uniforme ; à rouvrir si le play paraît encore
  petit à l'usage.

## Decisions

- La taille des icônes reste en `em` (proportionnelle à l'hôte), seule la
  constante change — pas de prop `size` tant qu'un seul ratio suffit.

## Gate status

- typecheck : ✅ (`pnpm gate` exit 0)
- tests (with coverage) : ✅ 1416 tests (aucun test de taille — attribut
  visuel, vérifié navigateur ; les specs n'assertent pas le CSS)
- mutation (Stryker) : **skippé — core intouché**
- biome / sheriff / knip / jscpd / impeccable / react-doctor : ✅

## State to resume from

- **Single next action** : merger la PR (branche `fix/icon-size`) — et la
  PR #134 (+ Section / Maj+M) toujours ouverte.
- Gotchas : rien — un attribut SVG dans `icon.tsx`.
