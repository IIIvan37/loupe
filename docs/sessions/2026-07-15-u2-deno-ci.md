# Session — 2026-07-15 — U.2 job CI deno sur supabase/functions

## Done

- **U.2 (roadmap v4, Lot U)** : job `edge-functions` dans `ci.yml` —
  `denoland/setup-deno@v2` puis `deno check` (avec `--config
  supabase/functions/deno.json`) + `deno lint` + `deno fmt --check` sur
  `mint-analyze-token/`. Pas de stack : seul réseau = djwt depuis deno.land.
  Timeout 5 min. Les trois commandes vérifiées vertes en local.
- La suite d'intégration (`index.test.ts`, 8 tests contre le stack live)
  reste locale — documentée au runbook, seedée par
  `scripts/seed-supabase-deno-harness.sh` (U.3).

## Not done / remaining

- Niveau supérieur optionnel de la roadmap (injecter les deux fetches et
  tester `handler` hors-ligne) : non pris — la suite live couvre déjà le
  handler, le job CI verrouille types/lint/format qui étaient le trou réel.
- Reste du Lot U : U.4 (cliquets jscpd 1,0 / Stryker break 90), U.5 (basses
  groupées).

## Decisions

- CI = statique seulement pour l'Edge Function ; l'intégration reste un
  rituel local documenté (le stack CI Supabase coûterait plus que le risque
  couvert).

## Gate status

- Les 3 commandes du job vertes en local ; gate web/serveur non affectées
  (changement ci.yml seul). Stryker skippé (core intouché).

## State to resume from

- **Single next action** : PR de `feat/u2-deno-ci`, vérifier le job sur la CI
  de la PR, merger. Puis U.4 (cliquets — 5 lignes de config).
- Gotchas : `deno lint`/`fmt` découvrent `deno.json` en remontant depuis la
  cible ; seul `check` a besoin du `--config` explicite.
