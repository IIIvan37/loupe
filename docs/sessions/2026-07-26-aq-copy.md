# Session — 2026-07-26 — aq-copy

## Done

- **Lot AQ — vocabulaire et copy** (PR #265, branche `feat/aq-copy`) :
  - AQ.1 lexique 3 niveaux : « Morceau trop volumineux » (4 messages),
    `chroma-idle`/`track-image` → « morceau », « stem » éradiqué du catalogue
    (« pistes », « Séparer les pistes avant l'export »).
  - AQ.2 : « key of {key} » → « Tonalité : {key} », « chart » → « grille »,
    « pitch » → « hauteur », « beta » → « bêta », infinitif partout
    (Glissez/collez/Déposez/Entrez/Connectez-vous corrigés) ; **Mix et Tap
    gardés** (termes métier assumés — décision).
  - Garde-fou : `packages/web/src/locales/copy-lexicon.spec.ts` (liste noire
    sur le catalogue, placeholders ICU strippés — `{pitch}` est un nom de
    variable). Rouge prouvé sur l'ancien catalogue (6/6 familles) avant
    `i18n:extract`.
- **Retours session (hors plan roadmap)** :
  - Majuscules : les 8 `text-transform: uppercase` du pattern eyebrow retirés,
    `--tracking-label` (pensé pour les capitales) supprimé avec ses 8 usages ;
    `.label` vide d'`undetected-stems` nettoyé (règle + classe).
  - Trim léger des textes longs : quota, `analysis.no-loops`,
    `chords.empty-hint`, tooltip vitesse. Le « pourquoi » des actions bloquées
    est conservé (`chords.detect-needs-grid`, reste du message quota).

## Not done / remaining

- Replay bundle utilisateur du Lot AP (croix rouge/⌘Q propre et sale,
  géométrie maximisée, titre natif avec ●) — côté utilisateur.
- Garde-fous beta (plafond de dépense Modal, re-seed codes legacy).

## Decisions

- « Mix » et « Tap » entrent au lexique comme anglicismes métier assumés
  (documenté dans l'en-tête de `copy-lexicon.spec.ts`).
- Trim « léger » : couper le décoratif, garder la raison d'un blocage.
- Plus aucun texte UI en capitales forcées ; la hiérarchie des micro-labels
  repose sur taille + couleur (`--dim`/`--faint`) seulement.

## Gate status

- typecheck: ✅
- tests (with coverage): ✅ (copy-lexicon 7/7 ; suite complète verte)
- mutation (Stryker, local, if core touched): **skippé — aucun fichier
  `@app/core` touché** (copy web + CSS + spec catalogue).
- biome / sheriff / knip / jscpd: ✅ (`pnpm gate` exit 0 après `check:fix`
  sur le formatage du nouveau spec)

## State to resume from

- **Single next action**: merger PR #265 ; ensuite rejouer le bundle AP côté
  utilisateur, puis cadrer le lot store Jotai (pilote separation+mixer).
- Gotchas : toute nouvelle copy passe sous `copy-lexicon.spec.ts` — un mot de
  la liste noire dans un message casse la suite ; après tout changement de
  copy, `pnpm --filter @app/web i18n:extract` (le spec le rappelle en message
  d'échec). Piège vécu : apostrophe française dans une string à quotes simples
  (`l'export`) casse l'extract Lingui — préférer les guillemets doubles.
