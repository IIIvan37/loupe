# Session — 2026-07-03 — i18n-lingui

## Done
- **All web UI copy goes through Lingui v6** (user-requested evolution), the
  canonical workflow: macros in components (`t({ id, message })`,
  `<Trans id="…">`, `msg()` at module level), **explicit semantic ids**
  (user's call), extraction to `src/locales/fr/messages.po` (source of
  truth, `sourceLocale: fr`, 113 messages), catalogs **compiled on import**
  by `@lingui/vite-plugin` — nothing generated on disk (user's call:
  generated artifacts stay out of git; here none exist at all).
- **Copy reworded to infinitive forms** (user's call — no tutoiement):
  « Reprendre un projet enregistré là où il a été laissé. », « … vérifier
  que le serveur local est lancé », « En ajouter depuis la barre de
  repères. », « Importer un morceau/fichier… », « Séparer les pistes… ».
- **Specs test by key** (user's question, settled by Lingui's official
  testing guidance: real i18n instance + provider wrapper, no mocking):
  every French literal in specs replaced by `i18n._('id', values)` under
  `I18nTestingProvider` — a copy change never breaks a test, interpolation
  is exercised for real, accessible names stay distinct.
- **WorkstationShell exploded into view regions** (user request):
  `ShellHeader` (identity + actions + banners + hidden file input),
  `ShellDialogs` (shortcuts + projects overlays), `ShellMain` (timeline +
  analysis), `ShellStage` (gutter + zoomable lanes). The shell keeps hooks
  and composition only (227 lines).
- Toolchain findings burned into config/docs:
  - `@vitejs/plugin-react` v6 **dropped the `babel` option silently** — the
    built bundle shipped the macro runtime (broken). Downgraded to v5
    (which officially peers with Vite 8); the web build now really strips
    macros (verified in `dist`).
  - Vitest doesn't run plugin-react's babel pass — the root vitest config
    gets a **dedicated minimal babel pass** (macro plugin only, scoped to
    `packages/web`, TS/JSX preserved).
  - `lingui extract` keeps existing msgstr — **`--overwrite` is mandatory**
    for the source locale when copy changes; wired into
    `pnpm --filter @app/web i18n:extract` (`--overwrite --clean`).
  - The `values:` key inside a macro descriptor is **silently dropped** —
    interpolation must use a template literal in `message` with simple
    identifiers (documented in the testing skill + CLAUDE.md).
- Conventions written down: CLAUDE.md (Lingui section),
  `react-testing-patterns` (key-based queries + wrapper), official
  `lingui-best-practices` skill vendored into `.claude/skills/` (from the
  link the user provided, via lingui/skills).
- Medium code review (3 finder agents + catalog-consistency scripts):
  no correctness bug; fixed « Mix » label left hardcoded, triple-repeated
  descriptors hoisted, server-status label made locale-reactive, catalog
  re-extracted after the shell split. Catalog audit: no inline↔po drift,
  no missing ids, duplicates byte-identical.

## Not done / remaining
- **Browser click-through pending — on the Mac** (no Chrome on this WSL2
  PC): visually skim every screen for copy regressions (header states,
  dialogs, mixer, shortcuts help), incl. the reworded strings.
- Second locale = add it to `lingui.config.ts`, translate the .po, load the
  catalog in `i18n.ts` + a switcher; runtime is already reactive.
- `eslint-plugin-lingui` doesn't apply (repo uses Biome) — no automated
  guard against future hardcoded literals; the CLAUDE.md rule and review
  are the guard for now.
- Restoring `@vitejs/plugin-react` v6 would need the web build to use its
  own babel pass for macros (like vitest does) or Lingui to ship an OXC
  plugin — deferred, v5 officially supports Vite 8.

## Decisions
- **Lingui canonical, explicit semantic ids, .po as the copy source of
  truth, no compiled artifacts in git** (all confirmed by the user).
- **Tests resolve keys against the real catalog — never mock i18n, never
  hardcode copy in specs** (Lingui official guidance; the user's
  interpolation-fragility concern is addressed by passing values the test
  already owns).
- **French copy uses infinitive forms** — no tutoiement/vouvoiement.

## Gate status
- typecheck: ✅ (0 errors)
- tests (with coverage): ✅ 425 passed / 425 (49 files)
- mutation (Stryker, local, if core touched): **skipped — core untouched**
- biome / sheriff / knip / jscpd / react-doctor / impeccable: ✅ all green
  (react-doctor: ShellStage extraction cured `no-giant-component`;
  `doctor.config.json` override for the spec-only `i18n-testing-provider`;
  NB: that file was accidentally clobbered mid-session and restored from
  git — it already existed with the `public/**` ignore)

## State to resume from
- **Single next action**: open the PR for `feat/i18n-messages` **based on
  `feat/dirty-session-guard`** (stacked on PR #36 — retarget to `main`
  once #36 merges), then browser-verify both slices on the Mac.
- Gotchas / half-done edits: none. Copy-change workflow: edit the
  `message:` in the component **and** run
  `pnpm --filter @app/web i18n:extract` (otherwise the stale .po msgstr
  wins at runtime); the gate does not yet check catalog sync.
