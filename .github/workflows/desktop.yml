name: Desktop

# AI.1: the Rust side of the Tauri shell (download.rs is a trust boundary —
# host allowlist, subprocess driving, executable bootstrap) must not live
# outside every gate. Path-filtered so the web/server pipelines stay fast.
on:
  push:
    branches: [main]
    paths:
      - 'packages/desktop/src-tauri/**'
      - 'crates/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rustfmt.toml'
      - '.github/workflows/desktop.yml'
  pull_request:
    paths:
      - 'packages/desktop/src-tauri/**'
      - 'crates/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rustfmt.toml'
      - '.github/workflows/desktop.yml'

permissions:
  contents: read

concurrency:
  group: desktop-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    name: Rust (fmt · clippy · tests)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      # The tauri crate links the platform webview even for `cargo test`.
      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev \
            libayatana-appindicator3-dev librsvg2-dev
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      # Workspace-wide (D4.a): the shared crates and the Tauri shell go
      # through the same sweep, from the repo root.
      - run: cargo fmt --all --check
      - run: cargo clippy --workspace --all-targets -- -D warnings
      - run: cargo test --workspace

  # D6: the distributed binary ships on Windows too, so its crates are tested
  # there — path separators, filesystem semantics, the `.exe` yt-dlp name.
  # Scoped to loupe-server + loupe-download (not the whole workspace): the
  # Tauri shell links WebView2 and is not what the local server distributes.
  windows:
    name: Rust Windows (loupe-server · loupe-download)
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy -p loupe-server -p loupe-download --all-targets -- -D warnings
      - run: cargo test -p loupe-server -p loupe-download
